{
	"name": "bots-against-alignment",
	"private": true,
	"type": "module",
	"packageManager": "bun@1.1.34",
	"scripts": {
		"dev:app": "bun scripts/free-ports.ts 5173 && cd app && DATABASE_URL=file:./dev.db bun run db:migrate && bun run dev -- --host 127.0.0.1 --port 5173",
		"dev": "bun scripts/dev-all.ts",
		"storybook": "bun scripts/free-ports.ts 6006 && cd app && bun run storybook",
		"storybook:build": "cd app && bun run storybook:build",
		"deploy": "vercel --prod"
	}
}
